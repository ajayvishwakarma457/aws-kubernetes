
apiVersion: v1
kind: Service
metadata:
  name: node-ser-1
  labels:
    name: node-ser-1.0
spec:
  selector: 
    app: second-app
  ports:
    - name: http
      protocol: 'TCP'
      port: 80
      targetPort: 8080
    # - name: https  
    #   protocol: 'TCP'
    #   port: 443
    #   targetPort: 443
  type: LoadBalancer
